{
  "name": "sspai-prometheus-metrics",
  "version": "0.0.1",
  "description": "A service to show the views and likes of sspai articles using prometheus metric format.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "format": "standard --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "standard": "^17.1.0"
  },
  "dependencies": {
    "axios": "^1.6.5",
    "express": "^4.18.2",
    "prom-client": "^15.1.0"
  }
}
